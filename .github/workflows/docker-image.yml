name: Docker Image CI

on:
  push:
    branches: [ "Lmashni-patch-1-docker-action" ]
  pull_request:
    branches: [ "Lmashni-patch-1-docker-action" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
